/** Contrato partilhado com SmartTask (`window.postMessage`) — não alterar sem actualizar o outro repo. */
export const WISH_SMARTTASK_HANDOFF_POST_MESSAGE_TYPE_V1 = "wish-smarttask-handoff-v1" as const;
